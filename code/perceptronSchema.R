# -----------------------------------------------------------------
# training perceptron in a dataset
# -----------------------------------------------------------------

perceptron.train = function(dataset, weights, lrn.rate) {

	epochs = 0
	error  = TRUE

	while(error) {

		# for each example

			# do the sum (v)
			# calculates the signal function
			# check if update is necessary

	}

}

# -----------------------------------------------------------------
# Evaluating new examples after the model was trained
# -----------------------------------------------------------------

perceptron.test = function(example, weights) {

}

# -----------------------------------------------------------------
# -----------------------------------------------------------------
